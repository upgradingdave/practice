/**
 * @param {number[]} arr
 * @return {number[]}
 */
let <{fn-name}> = function(arr) {
    if(arr === undefined || arr === null || arr.length <= 0) {
        return arr;
    }
    return arr;
};

module.exports = {
    <{fn-name}> : <{fn-name}>
};
