var expect = require('chai').expect;
var assert = require('chai').assert;

describe('Sanity Check', function(done) {
  it('should return true', function() {
    assert.equal(true, true, "true is true");
  });
});
